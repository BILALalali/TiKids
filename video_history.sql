drop table if exists video_history; create table video_history (id uuid default uuid_generate_v4() primary key, user_id uuid references auth.users not null, video_id text not null, title text, thumbnail_url text, video_url text, is_youtube boolean default false, watched_at timestamp with time zone default timezone('utc'::text, now()), created_at timestamp with time zone default timezone('utc'::text, now()), updated_at timestamp with time zone default timezone('utc'::text, now()));
